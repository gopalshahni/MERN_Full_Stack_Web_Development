# Today's Learning/Revision
- Name should be uppercase while creating and exporting function 
- we use <></> to return multiple elements from function
- packages are huge if created from create-react-app compared to vite-react 
- create-react-app injects js automaticaly to the the index file with the help of react-scripts while on the other hand vite js script is injected properly through script tag inside the index file 
- after learning  backend frontend seems to much easy at day 1 lets see what we will learn in upcoming far days 