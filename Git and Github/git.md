# Git Basics 
## git status show that your current folder is attached with git or git is tracking your folder or not 

## Atomic Commits are important 
- keep commit centric to one feature , one component at a single time 
- give a structure order 


## work flow of Git 
- Git init ,, initializer for git > git add specifi file / . > git commit to fix final files > git push to their master branch or another branch 

## here are some git commands 
- git status to check if the files are being tracked or not 
- git log to see the logs which are created by our actions of pushing the code

## .gitignore 
- here we define about which files should be ignored by simply enterung the file pname or path name , name will ignore akk files named with it but with path address it takes only the files from the showed path

## git secret files or hidden files which holds data like git logs, comments data and hooks or config files(global) etc.

## Every files hold a link to its previous changes like a pointer and there is HEAD which tells at which branch currently we are in the timeline/repo-line 

## git checkout to move another branch  
## git merge upgraded version is rebase because it don't add the merge commits in the commit history and also remember 1 and only one thing run rebase only on the another branch never run it on master branch  